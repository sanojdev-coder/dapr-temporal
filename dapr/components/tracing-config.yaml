apiVersion: dapr.io/v1alpha1
kind: Configuration
metadata:
  name: tracing-config
spec:
  tracing:
    samplingRate: "1"
    otel:
      endpointAddress: "tempo:4317"
      isSecure: false
      protocol: grpc
